name: Generate Changelog

on:
  # Chạy khi push vào main/master/develop (CHANGELOG chính thức)
  push:
    branches:
      - main
      - develop
  
  # Chạy khi tạo Pull Request (preview CHANGELOG)
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
  
  # Cho phép chạy manual
  workflow_dispatch:

jobs:
  changelog:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for changelog
          token: ${{ secrets.PAT_TOKEN || secrets.GITHUB_TOKEN }}
          ref: ${{ github.head_ref || github.ref_name }}  # Ensure correct branch
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install conventional-changelog-cli
        run: npm install -g conventional-changelog-cli
      
      - name: Create package.json for versioning
        run: |
          # Get latest tag or default to 0.0.0
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          VERSION=${LATEST_TAG#v}
          echo "Current version: $VERSION"
          
          # Create dummy package.json
          echo "{\"name\": \"greenedumap\", \"version\": \"$VERSION\"}" > package.json
          echo "✅ Created package.json with version $VERSION"

      - name: Generate CHANGELOG.md
        run: |
          # Generate changelog with Unreleased section
          npx conventional-changelog -p angular -i CHANGELOG.md -s -u
          echo "✅ CHANGELOG.md generated with Unreleased changes"
          echo "📋 First 30 lines of CHANGELOG.md:"
          head -n 30 CHANGELOG.md
      
      - name: Check if CHANGELOG changed
        id: changelog-check
        run: |
          if git diff --quiet CHANGELOG.md; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "⚠️ No changes detected in CHANGELOG.md"
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "✅ CHANGELOG.md has changes"
            echo "📝 Changes:"
            git diff CHANGELOG.md
          fi
      
      - name: Preview CHANGELOG for PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const changelog = fs.readFileSync('CHANGELOG.md', 'utf8');
            
            // Extract only the new changes (before first "---" or "## [")
            const lines = changelog.split('\n');
            let previewContent = [];
            let foundChanges = false;
            
            for (const line of lines) {
              if (foundChanges && (line.startsWith('## [') || line.startsWith('---'))) {
                break;
              }
              if (line.startsWith('## [Unreleased]') || foundChanges) {
                foundChanges = true;
                previewContent.push(line);
              }
            }
            
            const preview = previewContent.join('\n');
            
            const body = `## 📋 CHANGELOG Preview\n\n${preview}\n\n---\n*This preview will be added to CHANGELOG.md when merged.*`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
      
      - name: Configure Git
        if: github.event_name == 'push'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
      
      - name: Commit CHANGELOG.md
        if: github.event_name == 'push' && steps.changelog-check.outputs.changed == 'true'
        run: |
          git add CHANGELOG.md
          git commit -m "docs: update CHANGELOG.md [skip ci]"
          echo "✅ CHANGELOG.md committed"
      
      - name: Push changes
        if: github.event_name == 'push' && steps.changelog-check.outputs.changed == 'true'
        run: |
          echo "🚀 Pushing CHANGELOG.md to ${{ github.ref_name }}"
          git push origin HEAD:${{ github.ref_name }}
          echo "✅ Successfully pushed CHANGELOG.md"
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN || secrets.GITHUB_TOKEN }}
